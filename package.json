{
  "name": "etsy-bettersearch",
  "version": "0.14.1",
  "private": true,
  "type": "module",
  "description": "Build and test tooling for Etsy BetterSearch userscript, Chrome extension, and Firefox extension.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "ci": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
